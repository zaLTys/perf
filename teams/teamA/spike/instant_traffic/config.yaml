test_name: "teamA_spike_instant_traffic"

base_url: "https://test-api.example.com"

scenarios:
  spike:
    executor: "ramping-arrival-rate"
    startRate: 1
    timeUnit: "1s"
    stages:
      - duration: "10s"
        target: 200
      - duration: "20s"
        target: 200
      - duration: "10s"
        target: 0

thresholds:
  http_req_duration:
    - "p(95)<700"
