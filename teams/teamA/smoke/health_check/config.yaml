test_name: "teamA_smoke_healthcheck"

base_url: "https://test-api.example.com"

endpoints:
  health: "/health"

scenarios:
  smoke:
    executor: "constant-vus"
    vus: 1
    duration: "30s"

thresholds:
  http_req_duration:
    - "p(95)<300"
